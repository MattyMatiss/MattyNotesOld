надо бы сделать класс для добавления отсортированных (по?) mattygroupbox
должен заворачивать их в 

QHBoxLayout *HorizontalLayout;
	HorizontalLayout = new QHBoxLayout();
	HorizontalLayout->setSpacing(6);
	HorizontalLayout->setStretch(0, 0);
	HorizontalLayout->setSizeConstraint(QLayout::SetDefaultConstraint);
	ui.verticalLayout_4->addLayout(HorizontalLayout);
	MattyGroupBox* MyGroupBox = new MattyGroupBox();
	HorizontalLayout->addWidget(MyGroupBox);
	MyGroupBox->setTitle(QApplication::translate("MattyNotesClass", "Experiment", 0));
	HorizontalLayout->setObjectName("HorizontalLayoutExperiment");

ну и mattygroupbox должны принимать, хранить и применять кучу данных из базы

void fillFrame(MattyNote & ThisNote);
void fillFrame(MattyNote & ThisNote)
что-то там с круговыми зависимостями

заголовок заметки переходит в заголовок групбокса