1) MattyNote(QStringList RowFromDb);
подумать о формате строчки, последовательности полей
короче, сделать удобно и отказоустойчиво
если изменят базу не должно падать. в крайнем случае - не работать. говорить, почему не работает. лучше всего, конечно, чтобы чудесным образом все извлекалось. хотя...

2) void setEventTime(const QString & EventTime); // Warning! Input format must be: 00:00
	void setEventDate(const QString & EventDate); // Warning! Input format must be: 00.00.0000

разобраться с входным форматом
и с функцией починки из утилит

3) сет нул юзер дэйт энд тайм
